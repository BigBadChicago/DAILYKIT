/**
 * Layer 4. The FIVE LETTERS first session day, embedded. FIVE-LETTERS.md 31.
 *
 * The tutorial is a session mode that never counts (ARCHITECTURE2 section 40).
 * HEART is a familiar pool word in band 0: eleven accepted words share its
 * marks against the opening. tests/games/five-letters/module.test.ts holds it to
 * the pool and the band, so it cannot drift.
 */

export const FIRST_SESSION = { answer: "heart" } as const;
