Stats figures and score histogram. The current result uses the accent fill plus a `text` border, so it is distinguished by shape as well as color.

Values use tabular numerals. Bar width is proportional; the consumer supplies counts.
