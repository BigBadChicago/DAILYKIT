Read-only textarea holding the day's share text, in the mono board font.

`CopyBox({text, rows})`. Read-only so the content can be selected and copied but never edited; pair it with a Button to copy programmatically.
