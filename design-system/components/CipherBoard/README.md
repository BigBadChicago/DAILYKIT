CIPHER code-breaking board: past guesses with feedback text, four entry slots and a palette of keys.

`CipherBoard({history:[{code[4], feedback}], slots[4], palette[]})`. Slots and keys are 2.75rem (44px+); an empty slot is dashed. Feedback is text, never color.
