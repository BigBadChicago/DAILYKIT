Today's result card on the hub, shown once a game is played.

`HubDailyCard({title, block})`; `block` renders in the mono board font as preformatted text, matching how each game's own share block reads.
