Primary and secondary action button. Minimum 44px touch target; one primary per view.

Use `dk-button--primary` for the single main action (accent fill, accent-text label); everything else is the plain raised button. The consumer supplies the label and click handler. Focus is the 3px `focus` ring, never removed.
