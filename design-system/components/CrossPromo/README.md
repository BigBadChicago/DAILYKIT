One-line link to another game, set off by a hairline rule above it.

`CrossPromo({text, href})`. Text stays `text-dim`; it is a courtesy pointer, not a call to action, so it never takes the accent button treatment.
