Borderless 44px icon button used in the header row.

Always give it an `aria-label`; the glyph is text, not an image. Hover fills with `surface-raised`. Disabled uses `aria-disabled=true` at 40% opacity.
