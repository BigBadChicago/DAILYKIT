Current daily streak, centered under the hub header.

`HubStreak({value})`. A zero streak dims the number to `text-dim` instead of `text`, so a fresh visitor sees the same layout without a false sense of loss.
