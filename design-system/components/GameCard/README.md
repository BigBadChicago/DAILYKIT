Hub list card: a 4px left rail in the game accent hue, the uppercase name, 44px min height.

Each game sets its own `--accent` from the `game-*` tokens. The rail is decoration; the name carries meaning, so never use color alone.
