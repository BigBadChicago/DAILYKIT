Scrollable list of past puzzles by number and date, with a checkmark for completed ones.

`ArchiveList({items:[{number, date, done}]})`. Each row is a full-width 44px button; the checkmark is accent-colored and additional to the row's own affordance, never the only signal that a puzzle is done.
