Dialog on a `scrim` backdrop, max 460px wide, with a title row and close icon button.

The only surface that uses `shadow-modal`. Under forced colors it becomes a 1px CanvasText border. The consumer supplies focus trapping and Escape handling.
