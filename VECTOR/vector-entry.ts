/**
 * Layer 5. The VECTOR entry point. Exports the erased module and nothing else,
 * so the one cast in the codebase happens exactly once per game.
 */

import "../../games/vector/style.css";

export { default } from "../../games/vector/module.js";
